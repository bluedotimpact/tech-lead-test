# Docker environment variables
# This file is used when running the application inside Docker
# The DATABASE_URL is set in docker-compose.yml to use 'postgres' hostname

# For local development (outside Docker), use .env.local with:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/swe_lead_dev